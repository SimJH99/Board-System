spring:
  profiles:
    active: local #default

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.test.board.common.model
  configuration:
    map-underscore-to-camel-case: true

#    yml ??? dev, local ???? ??? ?? ? ?? ??? ??? ? ??? ??? ? ??.
